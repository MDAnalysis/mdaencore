name: mdaencore-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python=3.10
  - pip
  - cython

  # MDAnalysis
  - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  - MDAnalysisTests

  # Pip-only installs
  #- pip:
  #  - codecov

