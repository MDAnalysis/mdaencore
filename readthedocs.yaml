# readthedocs.yaml

version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "miniforge3-latest"

python:
  install:
    - method: setuptools
      path: .

conda:
  environment: docs/requirements.yaml
